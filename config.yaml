baseURL: "https://saan-volta.github.io/"
title: Saan
theme: hugo-theme-stack


    
markup:
  goldmark:
    extensions:
      passthrough:
        delimiters:
          block:
          - - \[
            - \]
          - - $$
            - $$
          inline:
          - - \(
            - \)
          - - $
            - $
        enable: true


params:
  math: true  # if you still want global math
  favicon: /favicon1.ico
  imageProcessing:
    cover:
      enabled: true
    content:
      enabled: false

  sidebar:
    subtitle: « one plus one equals three for significantly large values of one. »
    compact: false
    avatar:
      enabled: true
      local: true       # assume using static folder
      src: img/cat.png

  article:
    readingTime: false
